import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Optional: if you want to add global styles
import EmployeeForm from "./App";

ReactDOM.render(
  <React.StrictMode>
    <EmployeeForm />
  </React.StrictMode>,
  document.getElementById("root")
);
